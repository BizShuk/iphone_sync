# Project Memory

此目錄保存已完成工作的 retrospective、歷史操作與 durable decisions。

- 新增紀錄使用 `YYYY-MM-DD-<topic>.md`。
- 只記錄已發生且可由 repo 或執行結果驗證的內容。
- runtime data、照片 metadata、配對密鑰與原始監控 dump 不得寫入本目錄。
- 尚未執行的工作放入根目錄 `README.todo` 或 `plans/`，不放入 memory。
