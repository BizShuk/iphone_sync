# TODO

## 實機驗收 (Live-device Acceptance)

- [ ] 驗證 iCloud-only resource 跳過、RAW+JPEG、Live Photo、adjustment data 與大型影片
- [ ] 在 signed 實體 iPhone 驗證 `Delete After Sync` 預設關閉、enable warning、Photos allow / cancel、disable 清 pending 與 App relaunch persistence
- [ ] 驗證 background sync 只保存 pending IDs，App relaunch 後由 foreground `Delete N Synced Photos` confirmation 完成或安全取消
- [ ] 驗證背景 automatic run 的 `Operation Log` 事件在 App process 被系統終止後仍可於下次開啟時回查，且 500 筆上限與 clear 行為正確
- [ ] 驗證中斷續傳、iPhone 進入背景、Mac sleep、VPN、router isolation 與磁碟空間不足

## Automatic LAN Sync

- [ ] 驗證 30 分鐘 cadence：電池供電（未接電）時 iOS 仍會實際啟動 request、單次 run 由 OS expiration 中止且下次從 checkpoint 續傳
- [ ] 在 signed 實體 iPhone 驗證背景 window 不再重新 export 舊照片：`Operation Log` 應顯示既有照片以 `already present on the Mac` 一行帶過，且 `Last success` 由 background run 推進
- [ ] 驗證被 expiration 中止的 pass 於下一個 window 從中斷處接續，走完整個相簿後下一輪重新完整走訪
- [ ] 驗證 `Forget` receiver 後 ledger 與 album cursors 被清空，重新配對從完整 pass 開始

## Windows 11 Receiver

- [ ] Phase 8: iPhone 端驗證 iOS → Windows 互通（配對、SAS、Sync Now、Automatic LAN 場景）
- [ ] Phase 8: 中斷續傳、partial 磁碟、調整 storage mode、launch at login 驗證

## 打包與發佈 (Packaging & Release)

- [ ] 設定 Apple Developer ID secrets（`MAC_SIGN_IDENTITY` + `MAC_NOTARY_*`）讓 CI 產出公證後的 DMG/PKG，移除 Gatekeeper bypass 步驟
- [ ] Windows Authenticode / Azure Trusted Signing 簽章，消除 SmartScreen 警告
- [ ] 跑一次 `workflow_dispatch` draft release，於乾淨的 Mac 與 Windows 實機下載驗證一鍵安裝與首次啟動
